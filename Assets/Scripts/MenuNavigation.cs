using UnityEngine;

public class MenuNavigation : MonoBehaviour
{
    [SerializeField] 
    // Update is called once per frame
    void Update()
    {
        if (Input.GetKey(KeyCode.Tab))
        {

        }
    }
}
